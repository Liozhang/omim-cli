include requirements.txt
include omim_cli/version/version.json
# NOTE: do NOT ship any *.sqlite3. OMIM data is copyrighted by Johns Hopkins
# University and must not be redistributed. Users build their own local
# database with `omim-cli download` + `omim-cli update`.
recursive-exclude omim_cli/data *.sqlite3
global-exclude *.sqlite3
