# sdist extras; wheel uses pyproject.toml package-data.
# IMPORTANT: never ship *.sqlite3 — OMIM data is copyrighted by Johns Hopkins
# University; users build their own DB via `omim-cli download` + `omim-cli update`.
recursive-exclude omim_cli/data *.sqlite3
global-exclude *.sqlite3
